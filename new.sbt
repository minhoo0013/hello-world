import scala.math

def fibo(num: int) = {
    if(num < 2)
        return 1
    else
        return fibo(num-1) + fibo(num-2)
}

plintln(fibo(10))

def normal_() ={
    val r = new scala.util.Random
    r.nextInt()
    Array[double]
    new Array[int](100)
    1 to 100 



var randArr = Array[double](1000)

import scala.util.control.Break._


class MyClass(right: int,left: int) {
    //val?
    private val right_ = right
    private val left_ = left
    

}

object MyObject {

}
